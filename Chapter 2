Basic knowledge of Data product manager
2.1 The normal tools
2.1.1 Excel
1> Functions in date:
   DAY()
   MONTH()
   YEAR()
   DATE()
   TODAY()
   WEEKDAY()
   WEEKNUM()
2> Functions in Maths:
   PRODUCT() ×
   RAND(): return one random number between 0 and 1
   SUM()
   SUMIF()
   SUMPRODUCT()
3> Functions in statistic:
   LARGE(): the K's max
   SMALL(): the K's min
   MAX()
   RANK()
   COUNT()
   COUNTIF()
   AVERAGE()
   AVERAGEIF()
4> Functions in looking up
   CHOOSE()
   MATCH()
   COLUMN()
   INDEX()
   ROW()
   VLOOKUP(): (要查找的值，要在其中查找值得区域，区域中包含返回值的列号，精确匹配输入0/false或近似匹配输入1/TRUE)
   HLOOKUP()
   LOOKUP()
5> Functions in text
   FIND()
   SEARCH() LIKE  SEARCH("s","database") and return 7
   TEXT() LIKE TEXT(TODAY(),"MM/DD/YY")
   VALUE()
   LEFT()
   RIGHT()
   MID()
   LEN()
6> Functions in logisitic
   AND()
   OR()
   IF()
   FALSE()
   TRUE()
2.1.2 SQL
1> MySQL
   1.1> using mysql :
     mysql-uroot -p123456< testsql.sql
   1.2> using source:
     create dataase mydatabase;
     use mydatabase;
     set names utf8
     source/ home/ test.sql # 导入备份数据库
   1.3> using LOAD DATA
     LOAD DATA LOCAL INFILE ' source.txt' INTO TABLE mytable
2> Select data
   Select column AS from tablename
   where_
3> Data consolidation
  SELECT * FROM table1 INNER|LEFT|RIGHT JOIN table2 ON condition
4> Export data
  mysql -uroot -pdatapm mytable <mytable.sql;
5> View
  CREATE[or REPLACE][algorithm={undefined|merge|temptable}]
  VIEW view_name[(column_list)]
  as select_statement
  [with {cascade|local] check option]
6> Materialized views
   Aim to improve the speed of searching  like mbk.dm_view
   INSERT INTO TABLENAME AND SELECT
   namelist like mbk.dm_sessionize_dau_daily
2.1.3 R 
graphics\ lattice\ plotrix\ olotly\ REmap
www.r-project.org for reference
2.1.4 Product prototyping tool
   1> Axure

2.2 Product requirement management
2.2.1 Source of demand and judgment
2.2.2 Source pool management
2.2.3 Follow to realize

2.3 soft power
2.3.1 Fast grow up
2.3.2 communication
2.3.3 push project
2.3.4 data_aware
