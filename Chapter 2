Basic knowledge of Data product manager
2.1 The normal tools
2.1.1 Excel
1> Functions in date:
   DAY()
   MONTH()
   YEAR()
   DATE()
   TODAY()
   WEEKDAY()
   WEEKNUM()
2> Functions in Maths:
   PRODUCT() ×
   RAND(): return one random number between 0 and 1
   SUM()
   SUMIF()
   SUMPRODUCT()
3> Functions in statistic:
   LARGE(): the K's max
   SMALL(): the K's min
   MAX()
   RANK()
   COUNT()
   COUNTIF()
   AVERAGE()
   AVERAGEIF()
4> Functions in looking up
   CHOOSE()
   MATCH()
   COLUMN()
   INDEX()
   ROW()
   VLOOKUP(): (要查找的值，要在其中查找值得区域，区域中包含返回值的列号，精确匹配输入0/false或近似匹配输入1/TRUE)
   HLOOKUP()
   LOOKUP()
5> Functions in text
   FIND()
   SEARCH() LIKE  SEARCH("s","database") and return 7
   TEXT() LIKE TEXT(TODAY(),"MM/DD/YY")
   VALUE()
   LEFT()
   RIGHT()
   MID()
   LEN()
6> Functions in logisitic
   AND()
   OR()
   IF()
   FALSE()
   TRUE()
2.1.2 SQL
1> MySQL
   1.1> using mysql :
     mysql-uroot -p123456< testsql.sql
   1.2> using source:
     create dataase mydatabase;
     use mydatabase;
     set names utf8
     source/ home/ test.sql # 导入备份数据库
   1.3> using LOAD DATA
     LOAD DATA LOCAL INFILE ' source.txt' INTO TABLE mytable
